[project]
name = "autonomous-coding-agent"
version = "0.1.0"
description = "A minimal harness demonstrating long-running autonomous coding with the Claude Agent SDK"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "claude-agent-sdk>=0.1.0",
]
